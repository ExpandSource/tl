#1-1  shell#  사용자와 OS 사이의 명령 해석기

#어떻게 동작하나#
터미널에 명령 입력  →  쉘이 해석  →  OS 실행  →  결과 출력

#대표 쉘#
`bash`   대부분 배포판 기본. 안정적.
`zsh`    자동완성·플러그인 강력. 요즘 표준.
`sh`     최소 기능. 스크립트 호환용.

#확인·변경#
`echo $SHELL`            현재 쉘 확인
`chsh -s $(which zsh)`   기본 쉘 변경 (재로그인 필요)
*← tl p*   *1-1 / 5*   *tl n →*
